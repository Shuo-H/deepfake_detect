# main.yaml (no interpolation in defaults)
model_name: df_arena  # Central management: switch models here
load_type: gradio  # directory, gradio, or websocket
input_dir: samples/
server_name: 0.0.0.0
server_port: 7860
share: false
# WebSocket server configuration
websocket_host: 0.0.0.0
websocket_port: 8765